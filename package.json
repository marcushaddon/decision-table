{
  "name": "decision-table",
  "version": "1.0.0",
  "description": "Tools for creating, validating, documenting, and testing decision tables",
  "main": "dist/index.js",
  "scripts": {
    "test": "npx jest src",
    "build": "npm run build:schema && cp ./src/cmd/doc-schema.json ./dist/cmd/doc-schema.json && tsc -p ./tsconfig.json && chmod +x ./dist/cmd/index.js",
    "build:schema": "ts-node ./src/scripts/build-schema.ts"
  },
  "bin": {
    "check-table": "./dist/cmd/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.19",
    "@types/jest": "^29.2.4",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.5",
    "typescript-json-schema": "^0.55.0",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "jsonschema": "^1.4.1",
    "yaml": "^2.2.1"
  }
}
